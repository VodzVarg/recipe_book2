declare function reportWebVitals(onPerfEntry?: (entry: { 
    name: string;
    value: number;
    // Other properties might be available depending on the specific perf entry type
  }) => void): void;
  export default reportWebVitals;
  